# Production deployment gate

Requires founder approval, authenticated authority repositories, managed signing,
mTLS identities, encrypted durable persistence, destination and Emit Core
connections, conformance, incident response, backup/restore, independent security
validation, and clean evidence. `reference_deployment_not_production_authorized`
may never be promoted by configuration alone.
