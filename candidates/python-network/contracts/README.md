# Canonical contract index

The versioned JSON Schema 2020-12 artifacts in `v1/` are the local contract
spine. Protocol remains their authority. They are E1 artifacts until consumed
and validated by an independently pinned protocol repository.

Run `python scripts/generate_contracts.py --check` to validate and verify that
the generated Python binding is current.
