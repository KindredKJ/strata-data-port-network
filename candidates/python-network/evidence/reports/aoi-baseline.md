# AOI baseline

Current assigned AOI level: **none**. Immediate target: evidence-backed AOI-1.

Locally executed tests cover registered capability selection, tenant and
classification policy, budget and authority denial, explicit dependency plans,
alternate-capability replanning, evidence-bound memory, cross-tenant recall
denial, false content-hash rejection, and benchmark evidence requirements.
These validate partial AOI-1 foundations, not complete AOI-1 promotion.

Unexecuted promotion categories include authenticated NeedEnvelope interpretation,
ambiguity handling, real capability execution, Emit Core reconciliation,
complete evidence, repeated trials, explanation scoring, and authoritative
BRAINSTEM integration. AOI-2 remains blocked on multiple connected domains.
